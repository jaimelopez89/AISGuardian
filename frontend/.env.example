# Frontend Environment Variables
# Copy to .env and fill in your values

# Mapbox Access Token (required for map display)
# Get a free token at https://mapbox.com
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# Kafka REST Proxy URL (optional, defaults to /kafka which proxies to localhost:8082)
# VITE_KAFKA_REST_URL=http://localhost:8082
