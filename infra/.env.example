# AIS Watchdog Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# AISStream.io Configuration
# Sign up at https://aisstream.io to get a free API key
# ===========================================
AISSTREAM_API_KEY=your_aisstream_api_key_here

# ===========================================
# Kafka Configuration
# For local development, use localhost:9092
# For Aiven, use the service URI from the console
# ===========================================

# Local development (docker-compose)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Aiven Kafka (uncomment and configure when ready)
# KAFKA_BOOTSTRAP_SERVERS=your-kafka-instance.aivencloud.com:12345
# KAFKA_SSL_CA_CERT=/path/to/ca.pem
# KAFKA_SSL_CERT=/path/to/service.cert
# KAFKA_SSL_KEY=/path/to/service.key

# ===========================================
# Frontend Configuration
# ===========================================

# Mapbox (sign up at https://mapbox.com for free tier)
MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Kafka REST Proxy URL (for frontend)
KAFKA_REST_URL=http://localhost:8082

# ===========================================
# Ververica Cloud (Optional - for production Flink)
# ===========================================
# VERVERICA_API_TOKEN=your_ververica_token_here
# VERVERICA_NAMESPACE=default
# VERVERICA_DEPLOYMENT_TARGET=your-deployment-target

# ===========================================
# Application Settings
# ===========================================

# Rate limit for AIS ingestion (kb/s) - stay under Aiven's 250 kb/s limit
AIS_RATE_LIMIT_KBPS=200

# Geographic regions to monitor (JSON array of bounding boxes)
# Format: [[[lat_min, lon_min], [lat_max, lon_max]], ...]
# AIS_BOUNDING_BOXES=[[30.0, -6.0], [46.0, 36.0]]

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
